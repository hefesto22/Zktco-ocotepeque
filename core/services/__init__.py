# Core — services.
# Lógica de negocio. Una clase = una responsabilidad (SRP).
# Ejemplos: AuthService, PermissionService, AuditLogger, ZKTecoService, ReportService.
# Reciben repositorios por inyección en constructor — nunca instancian dependencias directamente.
